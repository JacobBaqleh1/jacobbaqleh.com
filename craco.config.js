module.exports = {
    babel: {
      presets: ["@babel/preset-env", "@babel/preset-react"],
      plugins: [
        // Add any custom Babel plugins here
      ],
    },
    webpack: {
      alias: {
        // Add any custom Webpack aliases here
      },
    },
    devServer: {
      // Add any custom dev server options here
    },
    // Add any other custom configuration options here
  };
  